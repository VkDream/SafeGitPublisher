<Window x:Class="SafeGitPublisher.Views.SetOriginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置 origin" Width="620" Height="320" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" Background="{StaticResource PageBg}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource SectionTitle}" Text="设置 / 修正 Remote origin" />

        <TextBlock Grid.Row="1" Text="Remote URL" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,4" />
        <TextBox Grid.Row="2" x:Name="UrlBox" FontSize="13" Padding="6,4" />

        <TextBlock Grid.Row="3" x:Name="HintText" Margin="0,8,0,0" TextWrapping="Wrap"
                   Foreground="{StaticResource TextSecondary}" />

        <CheckBox Grid.Row="4" x:Name="ReplaceCheck" VerticalAlignment="Top" Margin="0,14,0,0"
                  Content="origin 已存在，我确认将替换为上述 URL" Visibility="Collapsed" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" Width="90" Margin="0,0,10,0" Click="OnCancel" />
            <Button Content="确定" Style="{StaticResource PrimaryButton}" Width="90" Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>