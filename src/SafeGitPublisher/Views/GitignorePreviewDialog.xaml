<Window x:Class="SafeGitPublisher.Views.GitignorePreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=".gitignore 预览" Width="720" Height="560" WindowStartupLocation="CenterOwner"
        Background="{StaticResource PageBg}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource SectionTitle}" Text="生成 / 补充 .gitignore" />
        <TextBlock Grid.Row="1" Text="以下将写入 .gitignore（只追加缺失规则，不覆盖现有内容）。"
                   Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8" TextWrapping="Wrap" />

        <TextBox Grid.Row="2" x:Name="ContentBox"
                 IsReadOnly="True" AcceptsReturn="True" HorizontalScrollBarVisibility="Auto"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" Width="90" Margin="0,0,10,0" Click="OnCancel" />
            <Button Content="应用" Style="{StaticResource PrimaryButton}" Width="90" Click="OnApply" />
        </StackPanel>
    </Grid>
</Window>